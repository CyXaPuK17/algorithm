const {bubbleSort} = require('./bubble');
const {insertionSort} = require('./insert');
const {selectionSort} = require('./select');

module.exports = {
    bubbleSort,
    insertionSort,
    selectionSort
};